import 'package:flutter/material.dart';

class IngresosFotografia extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ingresos Fotografia'),
      ),
      body: Container(
        child: Center(
          child: Column(
            children: <Widget>[
              Text('Ingresos Fotografia')
            ],
          )
        )        
      )
    );
  }
  
}
